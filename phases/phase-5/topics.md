# Phase 5 — verification topics

Feed this list to the `verify-phase` skill. Add to it as the phase reveals dependencies you
did not anticipate; that is a normal part of using this repo, and the additions are the
interesting part of the git history.

- Valkey
- Redis licensing
- Upstash Redis free tier
- Cloudflare Queues free limits
- Cloudflare Cache API
- Cloudflare Durable Objects
- BullMQ
- k6
- PgBouncer / Neon pooling
- Neon compute-hour limits

## Free tiers this phase depends on

- Cloudflare Workers + Durable Objects
- Cloudflare Workers + Queues
- Cloudflare Workers + Upstash
- k6 + GitHub Actions

Check each one's **current** limits against its official pricing page. Free tiers die quietly.
